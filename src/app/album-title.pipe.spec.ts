import { AlbumTitlePipe } from './album-title.pipe';

describe('AlbumTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new AlbumTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
